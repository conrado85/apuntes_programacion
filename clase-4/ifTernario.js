//extructura basica.
/**a diferncia de un if tradicional, el if ternario se escribe de forma horizontal.
 * al igual que el if tradicional, tiene el mismo flujo(si esta condicion es verdadera,
 *  Hacer esto, de lo contrario lo otro), pero en este caso no hace falta escribir if y else
   */

// condicion? primera exprecion : segunda exprecion

let mayor = (10>20)? 'es mayor': 'no es mayor';
console.log(mayor);


let fruta ='pera';
let preferida = (fruta == 'manzana')? 'es mi fruta preferida': 'no me gusta la fruta';
console.log(preferida);